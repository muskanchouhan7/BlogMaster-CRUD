<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/show.css" />
    <title><%= post.title %></title>
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"/>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>
  </head>
  <body>
    <div class="card-container">
      <div class="card">
        <h2><%= post.title %></h2>

        <!-- image input -->
        <% if (post.image) { %>
        <img
          src="<%= post.image %>"
          alt="<%= post.title %>"
          class="post-image"/>
        <% } %>

        <p><%- post.content %></p>

        <div class="btnbox">
          <a href="/">
            <button class="btns btn1" style="font-size: 29px">🔙</button>
          </a>
          <a href="/posts/<%= post._id %>/edit">
            <button class="btns btn2">
              <i class="fa" style="font-size: 38px; color: rgb(67, 145, 247)">&#xf044;</i>
            </button>
          </a>
          <form
            action="/posts/<%= post._id %>?_method=DELETE"
            method="POST"
            style="display: inline">
            <button type="submit" class="btns btn3">
              <i class="material-icons" style="font-size: 33px; color: red">delete</i>
            </button>
          </form>
        </div>
      </div>
    </div>
  </body>
</html>
